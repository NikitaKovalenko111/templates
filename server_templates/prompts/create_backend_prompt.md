Ты — senior Go backend engineer.

Задача: реализовать MVP backend приложения строго по заданному шаблону.

=== КОНТЕКСТ ===
Функционал:
{FEATURES}

Интерпретация шаблона:

1. Повторяй существующие паттерны из проекта (handlers, services, repositories)
2. Если в шаблоне есть похожий модуль — копируй его структуру
3. Не придумывай новые архитектурные решения
4. Не добавляй новые слои, если их нет в текущем проекте
5. Следуй naming и стилю существующего кода

Правила:

- handler → принимает HTTP, валидирует, вызывает service
- service → бизнес-логика
- repository → работа с данными
- запрещено добавлять новые слои без необходимости

Найди в проекте наиболее похожий существующий модуль и реализуй новый функционал по аналогии с ним.
Если функционал можно реализовать проще — выбирай более простой вариант.

=== ТРЕБОВАНИЯ ===

1. Не отклоняться от структуры проекта
2. Не добавлять лишние абстракции
3. Реализовать только то, что есть в ТЗ
4. Явная обработка ошибок (без игнорирования)
5. Код должен быть простой и читаемый
6. Dependency injection через конструкторы

=== ЗАПРЕЩЕНО ===

- добавлять новые пакеты без необходимости
- создавать интерфейсы без реальной пользы
- делать “на будущее”
- усложнять архитектуру

=== ФОРМАТ ОТВЕТА ===

1. Структура проекта (дерево файлов)
2. Код по файлам
3. Краткое объяснение архитектуры

=== САМОПРОВЕРКА (перед выводом) ===
Проверь:

- соответствует ли структура шаблону
- нет ли лишних слоёв
- весь ли функционал из ТЗ реализован
- нет ли лишнего кода
