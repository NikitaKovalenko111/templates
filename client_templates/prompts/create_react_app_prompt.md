Ты — senior frontend engineer (React).

Задача: перенести готовую HTML/CSS верстку в React-приложение, строго сохранив структуру и архитектуру.

=== КОНТЕКСТ ===
Верстка находится в markup

Описание приложения:
...

Функции приложения:
...

=== ЦЕЛЬ ===
Перенести верстку в React без изменения внешнего вида и логики.

=== ШАБЛОН ПРОЕКТА ===
Структура:

- src/
    - components/
    - assets/
    - api/
    - pages/
    - styles/
    - redux/
        - features/
        - saga/
          store.ts

=== ПРАВИЛА ===

1. Структура компонентов:
    - каждая логическая секция = отдельный React-компонент
    - переиспользуемые блоки → components/
    - страница → pages/

2. Перенос HTML:
    - максимально сохранить исходную структуру
    - не менять классы (БЭМ сохраняется)
    - заменить:
        - class → className
        - for → htmlFor

3. SCSS:
    - использовать существующий SCSS без изменений
    - не переписывать стили без необходимости
    - подключать стили через import

4. Архитектура:
    - без лишних абстракций
    - без hooks, если нет логики

5. Чистота:
    - простой читаемый код
    - без дублирования
    - без лишних компонентов

6. Использовать библиотеки, подключенные в package.json

7. Строго следовать шаблону

---

=== ЗАПРЕЩЕНО ===

- менять структуру DOM без причины
- добавлять hooks “на будущее”
- переписывать SCSS в JS (styled-components и т.д.)
- усложнять компоненты
- добавлять бизнес-логику, если её нет в ТЗ

---

=== ФОРМАТ ОТВЕТА ===

1. Структура проекта
2. Разбиение на компоненты (кратко)
3. Код
4. Как подключены стили

---

=== САМОПРОВЕРКА ===

Перед выводом проверь:

- совпадает ли DOM со старой версткой
- сохранены ли все классы (БЭМ)
- нет ли лишних компонентов
- нет ли лишней логики
